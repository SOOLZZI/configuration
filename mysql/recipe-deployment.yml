apiVersion: apps/v1
kind: Deployment
metadata:
  name: recipe-mysql
  labels:
    app: recipe-mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recipe-mysql
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: recipe-mysql
    spec:
      containers:
      - name: mysql
        image: mysql:latest
        volumeMounts:
          - mountPath: /var/lib/mysql
            name: recipe-mysql-volume
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "1234"
        ports:
        - containerPort: 3306

      volumes:
      - name: recipe-mysql-volume
        hostPath:
          path: /volume/data/recipe  

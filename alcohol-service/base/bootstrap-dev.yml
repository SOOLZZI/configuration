spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${database.host}/${database.schema}?serverTimezone=Asia/Seoul
    username: ${database.username}
    password: ${database.password}

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
spring:
  application:
    name: configuration

  profiles:
    active: native

  cloud:
    config:
      server:
        git:
          uri: https://github.com/SOOLZZI/configuration.git
        native:
          search-locations: classpath:/config
        bootstrap: true
server:
  port: 8071

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:60601/harudb?serverTimezone=Asia/Seoul
    username: ${database.username}
    password: ${database.password}

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create